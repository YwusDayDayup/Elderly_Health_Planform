<?xml version="1.0" encoding="UTF-8"?>
<configuration>
  <include resource="org/springframework/boot/logging/logback/defaults.xml"/>

  <property name="CONSOLE_LOG_PATTERN"
            value="%clr(%d{yyyy-MM-dd HH:mm:ss.SSS}){faint} %highlight(%-5level) %msg%n"/>

  <include resource="org/springframework/boot/logging/logback/console-appender.xml"/>

  <root level="WARN">
    <appender-ref ref="CONSOLE"/>
  </root>

  <logger name="com.example.config.web.RequestLogFilter" level="INFO"/>
  <logger name="com.example.config.exception.GlobalExceptionHandler" level="WARN"/>
  <logger name="com.example.config.StartupAddressLogger" level="INFO"/>
</configuration>
